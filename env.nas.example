# === COPY THIS FILE TO .env ON THE NAS ===
# (Same folder as docker-compose.yml)
#
# Your PC wms/.env is ONLY for local dev (npm run dev).
# The NAS needs its own .env with the lines below.

POSTGRES_USER=wms
POSTGRES_PASSWORD=Oatmilk1769!
POSTGRES_DB=wms

AUTH_SECRET=techchefs-wms-change-me-to-32chars-min
APP_URL=https://warehouse.techchefstx.work

ADMIN_EMAIL=dispatch@techchefstx.com
ADMIN_PASSWORD=Oatmilk1769!
ADMIN_NAME=Dispatch
SEED_DEMO=true
