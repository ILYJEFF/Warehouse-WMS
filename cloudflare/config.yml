# Optional local tunnel config (use TUNNEL_TOKEN in docker-compose instead)
# Mount credentials.json and set tunnel ID before using.

tunnel: REPLACE_WITH_TUNNEL_ID
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: warehouse.techchefstx.work
    service: http://app:3090
  - service: http_status:404
